# Firebase Hosting deployment disabled - Now using Vercel
# This workflow is kept for reference but disabled
# Note: Deployment is now handled automatically by Vercel on every push to main
# Vercel auto-detects changes and deploys automatically (no GitHub Actions needed)

name: Deploy to Firebase Hosting (Disabled)

# Disabled workflow - will not run
on:
  workflow_dispatch: # Only manual trigger, but workflow is disabled via empty job

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Deployment is handled by Vercel."

